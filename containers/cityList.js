// import React from 'react';
// import {connect} from 'react-redux';
// import {CityItems } from '../components';
// import { CityList } from '../components';

// export default function cityListContainer({city}) {
//   console.log(city);
//   // const styles = useSelector(state => state.styles)

// 	return (
// 		<div>
// 			<h1>Styles list</h1>
// 			<CityList>
// 				{city.map(city => (
// 					<CityItems key={city.id} city={city} />
// 				))}
// 			</CityList>
// 		</div>
// 	);
// }

// export default connect((state) => ({city: state.city}))(cityListContainer)
